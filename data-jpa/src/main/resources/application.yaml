server:
  port: 8100
spring:
  application:
    name: @artifactId@
  datasource:
    url: jdbc:postgresql://localhost:5433/examples?currentSchema=examples_spring_hibernate
    username: postgres
    password: 190695
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        jdbc:
          batch_size: 5
        order_inserts: true
        order_updates: true
        generate_statistics: true
    open-in-view: false
  flyway:
    enabled: true
    schemas: examples_spring_hibernate